import { conCat } from '../../../helpers/con-cat';
import { module, test } from 'qunit';

module('Unit | Helper | con cat');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = conCat(42);
  assert.ok(result);
});
