import { arrAy } from '../../../helpers/arr-ay';
import { module, test } from 'qunit';

module('Unit | Helper | arr ay');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = arrAy(42);
  assert.ok(result);
});
