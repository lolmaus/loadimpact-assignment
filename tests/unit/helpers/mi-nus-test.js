import { miNus } from '../../../helpers/mi-nus';
import { module, test } from 'qunit';

module('Unit | Helper | mi nus');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = miNus(42);
  assert.ok(result);
});
